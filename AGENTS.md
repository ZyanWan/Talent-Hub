# 项目约定

## 1. 项目说明

Talent Hub 是简历筛选与电话确认的桌面应用：后端 Python（`app/`），前端 React + TypeScript（`frontend/src/`）。`frontend/dist` 是构建产物，由后端托管，不纳入版本控制。

事实来源是代码本身。项目结构、运行拓扑、参考文档与模型输入文本从 `docs/SOURCE_MAP.md` 入口按主题查阅 `docs/source-map/`。

开工前必读：

- 改动代码或数据契约前，先读 `docs/source-map/07-change-guide.md`：修改前检查流程、高风险“看似局部”改动、文档维护要求。
- 涉及字段、状态、并发、持久化、密钥或 Excel 契约时，同时读对应专题。
- 本文件是约定的唯一来源。仓库根的 `CLAUDE.md` 只有一行导入语句，把本文件接进 Claude Code 的项目记忆；不要在那边重复内容。

## 2. 验证命令

| 改动范围 | 命令 |
| --- | --- |
| 后端与 `tests/` | `python -m pytest` |
| 前端单元与组件 | `cd frontend && npm run test:unit` |
| 前端 API 契约 | `cd frontend && npm run test:contract` |
| 前端构建（后端托管 `frontend/dist`，改前端源码后需重建才生效） | `cd frontend && npm run build` |
| 语法冒烟 | `python -m compileall app` |
| Markdown | `npx markdownlint-cli2 "*.md" "app/**/*.md" "docs/*.md" "docs/source-map/**/*.md" "debug/**/*.md"` |

`docs/references/` 是保留的外部参考资料，不参与格式检查。

新环境首次跑后端测试前，先 `python -m pip install -r requirements-dev.txt`；测试依赖不在 `requirements.txt` 里，以免被打进安装包。

改完必须跑到通过为止。跑不过就报告执行的命令与关键输出，不得含糊声称“应该没问题”。

不得为了变绿而修测试：不放宽断言、不加 `skip`、不改期望值、不删失败用例。修实现，不修测试。

## 3. 规则冲突时的裁决顺序

1. 安全：不泄露密钥、不执行高危操作。
2. 用户当轮的明确指令；涉及安全风险时先提醒，由用户决定。
3. 本文件的规则。

规则之间冲突时：新增用户可感知的能力时，文档同步（第 8 节）优先于“只碰必须碰的东西”（第 6 节），纯内部改动以第 6 节为准；判断要不要提问以第 4 节的成本门槛为准，第 7 节的“独立推进”不构成跳过提问的理由。

## 4. 编码前先思考

不要臆测，不要掩盖困惑，把取舍摆在明面上。

- 有默认做法时选默认做法，在回答里用一行标注“假设：…”，然后继续推进。
- 只在猜错的返工代价高于打断你的代价时才停下来提问。够格的情况包括：需求存在实质不同的两种理解、改动不可逆、要动状态机或对外契约、上下文确实缺失。
- 提问时给推荐方案加一句备选，不要只并列选项让人挑。
- 有更简单的做法直接说；该反驳时就反驳。
- 改代码前先搜清所有调用点，根因修一次，不要在每个调用点各补一次。

## 5. 简洁优先

用最少的代码解决问题，不写投机性内容。

- 不多做用户没要求的功能。
- 不为一次性的代码做抽象：不写只有一个实现的接口、永不变化的配置项、给“以后”用的脚手架。
- 不提供未被要求的“灵活性”或“可配置性”。
- 不为不可能出现的场景做错误处理。
- 取值顺序：标准库 → 语言或平台原生能力 → 已装依赖 → 才自己写。
- 如果写了 200 行、本可写 50 行，重写它。

问自己：“资深工程师会说这过度复杂吗？”是的话，就简化。

## 6. 手术式改动

只碰必须碰的东西，只收拾自己造成的乱摊子。

编辑既有代码时：

- 不去“改进”相邻的代码、注释或格式。
- 不去重构没坏的东西。
- 遵循现有风格，哪怕换个写法你更顺手。
- 发现无关的死代码时提及即可，不要删除。

当你的改动制造出失孤对象时：

- 删除因你的改动而不再被使用的导入、变量、函数。
- 除非被要求，否则不要删除本已存在的死代码。

判定标准：每一行改动都应直接追溯到你收到的那条需求。

## 7. 目标驱动执行

定义成功标准，按第 2 节验证命令循环到通过为止。

把任务转化为可验证的目标：

- “补校验” → “先为非法输入写测试，再让测试通过”
- “修缺陷” → “先写一个能复现它的测试，再让测试通过”
- “重构 X” → “确保重构前后测试都通过”

多步骤任务先给出简要计划：

```text
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
3. [步骤] → 验证：[检查点]
```

只跑受改动影响的部分即可，不必每次全量。声明完成必须附证据：执行的命令加关键输出；没跑过不许说“已通过”。

## 8. 文档同步

功能变更与文档更新必须同时完成，禁止先改代码后补文档。

同步范围由“用户能否感知”与“契约是否变化”决定，不是所有改动都要全量扫一遍：

- 新增模块、能力或外部服务，或改变用户可感知的行为 → 更新 `README.md` / `README.en.md`（当前能力、技术特性、配置章节）与 `APP_GUIDE.md`（面向用户的操作步骤）；两份 README 内容保持一致。
- 修改代码结构、数据流、API、配置字段或状态机 → 更新 `docs/SOURCE_MAP.md` 入口及 `docs/source-map/` 对应专题。
- 新增用户可操作的配置项 → 在 README 与 APP_GUIDE 中给出逐步配置指引，说明界面入口在哪个位置，不假设用户熟悉平台。
- 纯内部改动（重命名、提取函数、修注释）不触发上述同步。
- 文档必须基于代码实现描述事实，不记录临时调试过程；故障证据放 `debug/`，稳定约束回写文档。

判断是否漏改：新用户能否只靠 APP_GUIDE 独立完成配置；`docs/SOURCE_MAP.md` 是否仍反映真实数据流与影响面。

## 9. 生产代码卫生

注释与文档只描述当前代码事实，使用现在时；不记录历史变迁，不引用已删除的实现。

- 注释与项目文档中不得出现“之前 / 原来 / 旧版 / 原版 / 由原…变来 / 重构前 / 迁移前 / 未复刻 / 已删除”这类描述实现历史的措辞。本条不适用于 `debug/`、`docs/references/` 下的转录、复盘与外部参考资料。
- 不引用已不存在的文件或符号：直接描述当前行为。
- 迭代修改直接改原文件，不留 v1/v2 双份命名（如 `*-v2.test.ts`）；旧版本不再使用时删除，不保留“第二版”。
- 不把对话指令、用户要求、评审意见的措辞写进生产代码或生产文档；约束只沉淀在 `AGENTS.md` / `README` / `docs/SOURCE_MAP.md` 等约定性文档中。

## 10. Markdown 格式

仓库根目录 `.markdownlint.json` 是 Markdown 格式的唯一约定，规则明细以该文件为准。

改动 Markdown 后运行第 2 节命令表中的 markdownlint 命令，受版本控制的文档应无告警。本文件不再复述具体规则，避免与配置文件脱节。

## 11. 密钥与安全边界

- 模型 API Key、飞书 webhook、语音服务凭证只从配置或环境变量读取，禁止写进代码、注释、日志、提交信息或文档。
- `.env` 已由 `.gitignore` 覆盖，不要为了调试把密钥临时贴进代码。
- 发现密钥出现在将被提交的内容里，先停下并报告。

## 12. 提交

- 未经要求不 commit、不 push、不建分支、不改写历史。
- 提交信息沿用本仓既有格式 `<type>: <中文描述>`，type 取 `feat` / `fix` / `refactor` / `docs` / `style`（界面与交互调整）。描述一律用中文，不使用 `Add feature` 这类英文祈使句，也不写 `update code` 这类无信息量的信息。
- 不用 `git add -A`，逐个指定文件路径；无关改动不混进同一个提交。
