# 招聘工作台

## 开发与构建

开发环境的依赖安装、手动启动步骤及 Windows / macOS 发行构建说明统一见 [README「快速开始（开发环境）」](README.md#快速开始开发环境)、[README「Windows 构建」](README.md#windows-构建)和 [README「macOS 构建」](README.md#macos-构建)。

Windows 开发环境完成 Python 与前端依赖安装后，可双击项目根目录的 `start-app.bat`：脚本启动后端与前端自动构建监听，浏览器随后打开应用。该脚本不执行 `npm ci`；首次启动或依赖变化后，先按 README 安装依赖。关闭 Backend 和 Frontend Watch 两个窗口即可停止应用。

## 首次配置

1. 点击顶栏右侧的设置按钮，打开「应用设置」。
2. 填写模型服务基础地址和模型名称。基础地址不含 `/chat/completions`，应用会自动追加该路径；地址不强制以 `/v1` 结尾。
3. Windows 在设置中填写 API Key；macOS 在启动应用前通过 `TALENT_HUB_API_KEY` 环境变量提供模型密钥。
4. 点击「测试模型链接」确认连接，再点击「保存配置」。

「请求超时（秒）」默认 180 秒，控制普通模型 HTTP 请求的每次尝试。筛选标准与单份简历的每轮模型调用分别允许最多 3 次和 2 次可重试传输尝试；JSON / 结构校验失败时可再发起一轮模型调用纠正。电话整理的每轮模型调用中，每次尝试至少使用 300 秒，允许最多 3 次可重试传输尝试；结构校验失败时也可再发起一轮。

## 简历筛选

简历筛选分为「生成并校准筛选标准」和「评估候选人」两个阶段：

1. 点击顶栏「新建」，选择「简历筛选」。
2. 在「岗位说明文本」中粘贴 JD，通过「候选人简历」添加一份或多份简历，然后点击「开始筛选」。
3. 应用创建任务、上传材料并根据 JD 生成筛选标准；此时尚未评估候选人。
4. 进入「校准筛选标准」后，检查岗位本质、硬门槛、A/B/C 条件、负向信号和加分项。按实际招聘口径修改后，点击「保存并开始筛选」。
5. 评估完成后，在结果页按 A/B/C 查看候选人、预览原始简历和筛选标准，并下载评估表格。修改已完成任务的筛选标准后，应用会对全部简历重新筛选。

默认使用 6 路候选人并发，上限为 12。每份简历进入一条评估流程，程序不发起独立的模型复核；遇可重试传输错误或 JSON / 结构校验失败时可能再次请求模型。结果经原文证据校验后输出，证据不足或核心维度存疑时转为待确认，由 HR 复核。

单份简历解析或模型请求失败时，系统会记录该文件的错误并继续处理其他简历。若候选人结果已保存、但 Excel 生成或校验等收尾步骤失败，结果页会展示已保留结果和「重新开始」按钮；评估表格下载、追加简历、修改标准和飞书通知在正式产物生成前不可用。「重新开始」会复用已保存的成功结果，继续处理未完成部分并重新生成正式产物。

## OCR 配置

文本型 PDF、DOCX、TXT 和 Markdown 无需 OCR。扫描 PDF 与图片简历需要安装 Tesseract；应用会从 `TESSERACT_CMD`、`PATH` 和平台常见路径自动探测，探测失败时再在设置中填写程序路径。中文简历应安装 `chi_sim` 语言包，应用会优先使用 `chi_sim+eng`。macOS 可通过 `brew install tesseract tesseract-lang` 安装。

## 本机数据

设置与任务默认保存在用户本机（Windows：`%LOCALAPPDATA%\TalentHub`；macOS：`~/.local/share/TalentHub`）。Windows 上模型 API Key、ASR Key 与飞书签名密钥使用当前用户的 DPAPI 加密；macOS 上分别通过 `TALENT_HUB_API_KEY`、`TALENT_HUB_ASR_API_KEY`、`TALENT_HUB_FEISHU_SIGN_SECRET` 环境变量配置。应用接口只监听回环地址，并要求所有 API 请求携带每次启动生成的本地会话令牌。

简历任务保留原始 JD 与简历、解析后的 JD、筛选标准、解析清单、结构化评估结果和已校验的 Excel；解析后的简历文本仅在「保留解析文本」开启时保存。电话任务保留原始录音、转写文本和整理档案。若关闭应用时仍有任务运行，重新打开后会标记为中断，可重新发起处理。

任务记录侧边栏分为“最近”和“已归档”。归档任务仍可查看和下载，也可恢复到最近任务；永久删除会移除该任务在本机保存的全部材料与结果，且无法撤销。运行中的任务不能归档或删除。

## 电话确认记录

电话确认记录支持一次选择多个候选人的电话录音，应用自动完成语音转写，并逐人整理情况说明，供 HR 校对和下载留档。

1. 先配置语音转写密钥：Windows 在顶栏右侧「设置」的「本地处理」分组填写 ASR API Key；macOS 在启动应用前设置 `TALENT_HUB_ASR_API_KEY`。
2. 点击顶栏「新建」，选择「电话确认」。填写可选的任务标题、岗位名、关联筛选岗位和软性素质关注项。
3. 选择一个或多个录音，支持 `.m4a`、`.wav`、`.mp3`、`.ogg`、`.opus`，单个文件不超过 100MB。
4. 点击「开始整理」。应用此时创建任务、上传录音并开始转写和 AI 整理；点击前不会单独创建任务。标题留空时使用日期生成，并随界面语言显示，手动标题保持原文。
5. 整理完成后逐人打开记录，校对候选人姓名、说明文字与字段，并下载对应的 Markdown 档案。

「应用设置」默认关闭「生成电话快筛详情（问答原文）」；关闭时可减少模型输出长度与整理耗时，需要通篇问答原文时可开启。

语音转写使用火山引擎 ASR 服务。原始录音内容会经网络发送至火山引擎 ASR 完成转写；转写文本会发送至用户配置的模型服务以生成电话整理结果。原始录音、转写文本与整理档案同时保存在本机数据目录。

电话确认任务同样按「最近」与「已归档」分类管理，支持归档、恢复与删除；删除会一并移除录音、转写文本与整理档案，且无法撤销。运行中的任务不能归档或删除。

## 飞书推送

开启后，简历筛选完成时会推送一条业务总览，电话整理完成时会按候选人逐条推送整理记录，无需手动转发。追加任务只通知尚未成功推送的新结果；修改筛选标准后的全量重筛会作为新版本通知。

### 在飞书里创建自定义机器人（首次）

以下步骤属于飞书平台操作，入口名称以飞书当前客户端为准；Talent Hub 只负责使用最终生成的 Webhook 和可选签名密钥。

1. 在飞书桌面客户端进入目标群。
2. 打开群设置，进入「群机器人」，选择「添加机器人」。
3. 选择「自定义机器人」，设置名称（如「Talent Hub」）并添加。
4. 复制弹出的 **Webhook 地址**（形如 `https://open.feishu.cn/open-apis/bot/v2/hook/xxxx`）。

> Talent Hub 支持飞书签名校验，但不会自动满足自定义关键词或 IP 白名单规则。启用这些飞书侧规则前，应确认应用发出的消息和请求来源符合相应条件。

### 在应用里配置

1. 点击顶栏右侧的设置按钮，打开「应用设置」弹窗。
2. 在「飞书推送」区块：
   - 勾选「任务完成后自动推送结果到飞书群」；
   - 粘贴 Webhook 地址；
   - 若开启了「签名校验」，Windows 上把签名密钥填入「飞书推送 · 签名密钥」（未开启则留空）；macOS 上使用 `TALENT_HUB_FEISHU_SIGN_SECRET` 环境变量；
   - 点「测试飞书链接」，群里收到「Talent Hub 飞书推送测试成功」即表示配置正确；
   - 点「保存配置」。

### 推送内容

- **简历筛选完成**：每轮一条总览，显示本轮提交文件数、成功评估人数、处理失败数、仅基于成功评估结果的 A/B/C 分布，以及最多 5 位 A/B 候选人的姓名、结论和一句话判定。追加简历时还会显示岗位累计成功评估人数，已推送结果不会重复出现；全部为 C 时只显示统计并提示当前没有建议优先处理的候选人。
- **电话初筛完成**：每位候选人一条消息，包含任务标题、候选人姓名、「AI 初步整理，请 HR 校对后使用」提示和应用内文本型整理记录。若已开启「生成电话快筛详情（问答原文）」且内容已进入整理记录，也会随正文发送；单条消息超限时会截断并提示回到 Talent Hub 查看完整记录。

外发前，程序按常见手机号、座机号和邮箱格式自动替换敏感信息；特殊写法可能无法识别，启用推送前仍需确认消息适合发送到目标群。电话消息不会额外附加转写原文、事实清单或引文字段。临时网络错误、HTTP 429 和 5xx 会有限重试；最终推送失败只在任务中显示提示，不改变业务处理状态。

若简历筛选的自动推送失败，打开对应的已完成结果页，点击候选人表格上方的「重试飞书通知」。应用只补发仍待通知的简历结果，并明确提示本次已发送或未发送。电话任务页不提供手动重试入口。

## 结果边界

应用会核验模型给出的简历事实依据，并由程序统一确定等级：硬条件或核心维度有明确不匹配时为「不推进（C）」；没有明确不匹配但存在关键信息未知时为「电话确认（B）」；全部硬条件和核心维度通过时为「优先约面（A）」。软性偏好只参与同等级排序，不会改变等级。模型提示词禁止使用年龄、性别、民族、籍贯、婚姻和生育状况进行评价或排序，程序同时按内置受保护属性关键词过滤硬门槛、A/B/C 条件和负向信号；该防护不能替代人工偏差复核。电话整理页会在每个字段旁显示模型根据通话语义给出的「已确认」「含糊」或「通话未提及」；程序保留通过结构校验的客观记录和软性素质评价，事实引用仅用于尝试定位录音。模型结果仍应由 HR 复核。
