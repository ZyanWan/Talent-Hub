# 招聘工作台

## 开发环境启动

Windows 一键启动：双击项目根目录的 `start-app.bat`，弹出两个窗口（Backend 后端、Frontend Watch 前端监听）后浏览器自动打开应用。日常修改前端代码时，Watch 窗口会自动重新构建，回到页面刷新即可看到新内容；关闭两个窗口即停止应用。

以下为手动方式。启动前需先构建前端（生产前端为 React + TypeScript，构建产物由后端直接托管，详见 README「快速开始（开发环境）」）：

```powershell
cd frontend
npm ci
npm run build
```

再启动后端：

```powershell
python -X utf8 -m app.main
```

应用仅监听 `127.0.0.1`，启动后会打开默认浏览器。首次使用时填写模型服务的 API 地址、API Key 和模型名称。兼容提供 `/chat/completions` 与 JSON 输出的 OpenAI 格式服务。

默认使用 6 路并发。每位候选人只做一次模型评估，结果经原文证据校验后直接输出；证据不足或核心维度存疑的候选人在结果中标记待确认，由 HR 校对时自行判断，不执行额外的模型复核。

任务运行页会显示已用时间与处理速率。并发上限为 12；具体可用并发取决于模型服务商的限流策略，遇到限流时应适当调低。每次模型请求的超时上限由「设置」中的「请求超时（秒）」控制，默认 180 秒；筛选标准生成最多尝试 3 次，单份候选人评估的模型传输请求最多尝试 2 次。

单份简历解析或模型请求失败时，系统会记录该文件的错误并继续处理其他简历；只要正式结果能够生成，就会进入结果页并同时展示成功候选人与失败信息。若候选人结果已保存、但 Excel 生成或校验等收尾步骤失败，应用会直接展示已保留的候选人结果和「重新开始」按钮；此时评估表格下载、追加简历、修改标准和飞书通知暂不可用。点击「重新开始」后，系统会复用已保存的成功结果，继续处理未完成部分并重新生成正式产物。

## OCR 配置

文本型 PDF、DOCX、TXT 和 Markdown 无需 OCR。扫描 PDF 与图片简历需要安装 Tesseract，并在设置中填写 Tesseract 程序路径。中文简历应安装 `chi_sim` 语言包；应用会优先使用 `chi_sim+eng`。macOS 可通过 `brew install tesseract tesseract-lang` 安装，常见路径为 `/opt/homebrew/bin/tesseract` 或 `/usr/local/bin/tesseract`。

## Windows 构建

构建机需要 Python、项目依赖、PyInstaller；生成安装包还需要 Inno Setup 7 或 6。

```powershell
python -m pip install -r requirements-build.txt
.\scripts\build_windows.ps1
```

便携版和安装包输出到 `release\<version>\`。最终用户不需要安装 Python；Tesseract 仅在处理扫描件或图片简历时需要。

构建脚本会生成应用图标和 Windows 版本信息，并对便携版执行健康检查与首页启动烟测。面向组织分发前应使用受信任的代码签名证书签署 EXE 和安装包，避免 Windows SmartScreen 将未知发布者标记为风险。

## macOS 构建

macOS 产物必须在 macOS 上构建。先显式安装构建依赖，再运行脚本：

```bash
python -m pip install -r requirements-build.txt
bash scripts/build_macos.sh
```

脚本会生成 `dist/TalentHub.app` 和 `release/<version>/macos/TalentHub-macOS-<version>.zip`，并执行本地启动烟测。当前 macOS 包未签名、公证；组织分发前应补充 Apple Developer ID 签名与公证。

发布目录会包含 `THIRD_PARTY_NOTICES.txt` 和 `THIRD_PARTY_LICENSES`，记录应用直接及传递依赖的许可证文件。PDF 处理采用 pypdf、pdfplumber 和 pypdfium2，不打包 PyMuPDF。

## 本机数据

设置与任务默认保存在用户本机（Windows：`%LOCALAPPDATA%\TalentHub`；macOS：`~/.local/share/TalentHub`）。Windows 上模型 API Key、ASR Key 与飞书签名密钥使用当前用户的 DPAPI 加密；macOS 上分别通过 `TALENT_HUB_API_KEY`、`TALENT_HUB_ASR_API_KEY`、`TALENT_HUB_FEISHU_SIGN_SECRET` 环境变量配置。应用接口只监听回环地址并要求每次启动生成的本地会话令牌。

每个任务保留 JD、原始简历、筛选标准、解析清单、结构化评估结果和已校验的 Excel。若关闭应用时仍有任务运行，重新打开后会标记为中断，可重新发起筛选。

任务记录侧边栏分为“最近”和“已归档”。归档任务仍可查看和下载，也可恢复到最近任务；永久删除会移除该任务在本机保存的全部材料与结果，且无法撤销。运行中的任务不能归档或删除。

## 电话确认记录

电话确认记录支持一次上传多个候选人的电话录音，应用自动完成语音转写，并逐人整理出一份情况说明文字，HR 校对后可下载留档。

使用流程：在顶栏「新建」中选择「电话确认记录」，填写任务标题与岗位名称后创建任务；在任务页上传录音，支持 `.m4a`、`.wav`、`.mp3`、`.ogg`、`.opus` 格式，单个文件不超过 100MB，同一任务可上传多位候选人的录音；点击「开始整理」后，应用逐个录音完成转写与信息整理，对无法在转写原文中核对的内容自动标记为含糊；整理完成后逐人校对说明文字，每位候选人对应一份可下载的 Markdown 档案。设置中默认关闭「快筛详情（问答原文）」以缩短整理耗时，需要通篇问答原文时可在设置中开启。

语音转写使用火山引擎 ASR 服务。Windows 上在「设置」的模型配置中填写「语音转写密钥」（ASR API Key），并使用当前用户的 DPAPI 加密后保存在本机；macOS 上通过 `TALENT_HUB_ASR_API_KEY` 环境变量配置。

上传的录音会经网络发送至语音识别服务完成转写；转写文本会发送至用户配置的模型服务用于生成电话整理结果。转写文本与整理档案会保存在本机数据目录。

电话确认任务同样按「最近」与「已归档」分类管理，支持归档、恢复与删除；删除会一并移除录音、转写文本与整理档案，且无法撤销。运行中的任务不能归档或删除。

## 飞书推送

开启后，简历筛选完成时会推送一条业务总览，电话整理完成时会按候选人逐条推送整理记录，无需手动转发。追加任务只通知尚未成功推送的新结果；修改筛选标准后的全量重筛会作为新版本通知。

### 在飞书里创建自定义机器人（首次）

1. 使用**电脑上安装的飞书客户端**（网页版和手机端没有此入口）。
2. 进入目标群（建议先建一个专门群，如「招聘通知」）→ 点右上角 **「⋯」/设置图标** → **群设置** → **群机器人** → **添加机器人**。
3. 在弹出的对话框里，找到 **「自定义机器人」**（它在应用机器人列表之外，可能在对话框底部或固定位置）→ 设置名称（如「Talent Hub」）→ **添加**。
4. 复制弹出的 **Webhook 地址**（形如 `https://open.feishu.cn/open-apis/bot/v2/hook/xxxx`）。

> 安全设置建议：**只勾选「签名校验」**（更安全），不要勾选「自定义关键词」和「IP 白名单」。若勾选了「签名校验」，创建时显示的签名密钥只在当时可见，请立即复制保存。

### 在应用里配置

1. 打开应用的「设置」弹窗（右上角模型配置按钮）。
2. 在「飞书推送」区块：
   - 勾选「任务完成后自动推送结果到飞书群」；
   - 粘贴 Webhook 地址；
   - 若开启了「签名校验」，Windows 上把签名密钥填入「飞书推送 · 签名密钥」（未开启则留空）；macOS 上使用 `TALENT_HUB_FEISHU_SIGN_SECRET` 环境变量；
   - 点「测试飞书链接」，群里收到「Talent Hub 飞书推送测试成功」即表示配置正确；
   - 点「保存配置」。

### 推送内容

- **简历筛选完成**：每轮一条总览，显示本轮提交文件数、成功评估人数、处理失败数、仅基于成功评估结果的 A/B/C 分布，以及最多 5 位 A/B 候选人的姓名、结论和一句话判定。追加简历时还会显示岗位累计成功评估人数，旧结果不会重复出现；全部为 C 时只显示统计并提示当前没有建议优先处理的候选人。
- **电话初筛完成**：每位候选人一条消息，包含任务标题、候选人姓名、「AI 初步整理，请 HR 校对后使用」提示和应用内文本型整理记录。若已开启「快筛详情（问答原文）」且内容已进入整理记录，也会随正文发送。

外发内容会隐藏手机号、座机号和邮箱；电话消息不会额外附加转写原文、内部事实或引文。单条记录过长时会安全截断并提示在 Talent Hub 查看完整内容。临时网络错误、HTTP 429 和 5xx 会有限重试；最终推送失败只在任务中显示提示，不改变业务处理状态。

若自动推送失败，打开对应的已完成简历筛选结果页，点击候选人表格上方的「重试飞书通知」；电话任务则进入任务详情，点击标题右侧的同名按钮。应用只补发仍待通知的结果，并明确提示本次已发送或未发送。

## 结果边界

应用会核验模型给出的简历原文引文，允许基于完整经历合理推断；只有存在关键事实二义（两种解读都可能且电话会改变结论）的候选人才进入「电话确认（B）」，并对证据不足的 A 类自动降档。模型结果仍应由 HR 复核。筛选准确性取决于 JD 清晰度、简历可解析质量、所选模型能力和用人部门口径；不能承诺所有岗位和样本达到固定准确率。
